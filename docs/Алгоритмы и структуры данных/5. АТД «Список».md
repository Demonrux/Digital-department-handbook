# АТД «Список»

Для доступа к месту расположения элемента в списке можно использовать не только разряд. Если список `S` реализован на основе односвязного или двусвязного списка, гораздо удобнее и эффективнее работать с **узлами** — они определяют «место» в списке напрямую, без необходимости пересчитывать разряды.

## Содержание

1. [Почему узлы лучше разрядов](#почему-узлы-лучше-разрядов)
2. [Понятие позиции](#понятие-позиции)
3. [Методы АТД «список»](#методы-атд-список)
4. [Реализация на C](#реализация-на-c)
5. [Стандартные реализации в других языках](#стандартные-реализации-в-других-языках)

---

## Почему узлы лучше разрядов

Пусть `S` — линейный список, реализованный с помощью двусвязного списка. Требуется определить методы, которые принимают в качестве параметров **узлы списка** и возвращают значения этих узлов. Такие методы работают значительно быстрее методов, основанных на разрядах: чтобы найти разряд в связном списке, необходим **последовательный просмотр** всех элементов от начала до конца с подсчётом пройденных. Это `O(n)`.

Если же у нас есть ссылка на узел, удаление элемента выполняется за `O(1)`: достаточно перейти к узлу, удалить его и перенаправить ссылки `next` и `prev` соседних узлов. Аналогично вставка нового элемента `e` за `O(1)` выполняется операцией `insertAfterNode(v, e)` — мы просто «вплетаем» новый узел после указанного.

Однако здесь возникает проблема **инкапсуляции**. Пользователю не следует модифицировать внутреннюю структуру списка без необходимости. Такая модификация возможна, если предоставленная пользователю ссылка на узел даёт доступ к полям `next` и `prev`. Это нарушает абстракцию.

## Понятие позиции

Чтобы абстрагироваться от конкретной реализации и унифицировать разные способы хранения элементов, вводят понятие **позиции** в списке. Позиция формализует традиционное понятие «места» элемента, связанного с другими элементами в списке. Она позволяет использовать одну и ту же реализацию как для односвязного, так и для двусвязного списка, не нарушая принципов объектно-ориентированного программирования.

Список в этой схеме рассматривается как **контейнер элементов**, хранящихся в определённых позициях, и эти позиции **линейно упорядочены**.

Позиция сама по себе — это абстрактный тип данных, поддерживающий простой метод:

**`element()`** возвращает элемент, хранящийся в данной позиции. Вход: нет. Выход: объект.

Позиции всегда определяются **относительно соседей**. В списке позиция `p` всегда следует «за» некоторой позицией `q` и «перед» некоторой позицией `s`, если `p` не первая или последняя. Позиция `p`, связанная с элементом `e` в списке `S`, **остаётся неизменной** даже при изменении разряда `e` — до тех пор, пока `e` явно не удалён. Более того, позиция не изменится, даже если заменить элемент `e` на другой. Эти особенности позволяют определять множество методов, использующих позиции как параметры и возвращающих объекты, хранящиеся в позициях.

## Методы АТД «Список»

Используя концепцию позиции для инкапсуляции узла, опишем АТД «Список». Он поддерживает следующие методы над списком `S`.

**`first()`** возвращает позицию первого элемента списка `S`; если список пуст, выдаётся сообщение об ошибке. Вход: нет. Выход: позиция.

**`last()`** возвращает позицию последнего элемента списка `S`; если список пуст — ошибка. Вход: нет. Выход: позиция.

**`isFirst(p)`** возвращает логическое значение: является ли позиция `p` первой в списке. Вход: позиция. Выход: логическое значение.

**`isLast(p)`** возвращает логическое значение: является ли позиция `p` последней. Вход: позиция. Выход: логическое значение.

**`before(p)`** возвращает позицию элемента, предшествующего элементу позиции `p`; если `p` первая — ошибка. Вход: позиция. Выход: позиция.

**`after(p)`** возвращает позицию элемента, следующего за элементом позиции `p`; если `p` последняя — ошибка. Вход: позиция. Выход: позиция.

Эти методы позволяют обращаться к смежным позициям, начиная с конца или с начала, и постепенно перемещаться по всему списку. Позиции можно считать узлами списка, но в методах нет явных ссылок на объекты узлов или их поля `next` и `prev` — детали реализации скрыты.

Помимо общих методов `size` и `isEmpty`, АТД «Список» описывает методы **обновления**:

**`replaceElement(p, e)`** замещает элемент в позиции `p` на `e` и возвращает прежний элемент. Вход: позиция и объект. Выход: объект.

**`swapElements(p, q)`** меняет местами элементы в позициях `p` и `q`. Вход: две позиции. Выход: нет.

**`insertFirst(e)`** вставляет новый элемент `e` в начало списка и возвращает его позицию. Вход: объект. Выход: позиция.

**`insertLast(e)`** вставляет новый элемент `e` в конец списка и возвращает его позицию. Вход: объект. Выход: позиция.

**`insertBefore(p, e)`** вставляет новый элемент `e` перед позицией `p`; если `p` первая — ошибка. Вход: позиция и объект. Выход: позиция вставленного элемента.

**`insertAfter(p, e)`** вставляет новый элемент `e` после позиции `p`; если `p` последняя — ошибка. Вход: позиция и объект. Выход: позиция вставленного элемента.

**`remove(p)`** удаляет из `S` элемент в позиции `p`. Вход: позиция. Выход: удалённый элемент.

АТД «Список» позволяет рассматривать упорядоченные по местоположению коллекции объектов, **не учитывая**, каким именно образом представлены эти места.

## Реализация на C

В C нет встроенного списка — его реализуют вручную через `struct` и указатели. Классический подход — **двусвязный список** с сигнальными (sentinel) узлами. Сигнальные узлы упрощают код: не нужно отдельно обрабатывать вставку в пустой список или удаление первого/последнего элемента.

```c
#include <stdio.h>
#include <stdlib.h>

// Узел двусвязного списка
typedef struct Node {
    int value;              // данные (для простоты — int)
    struct Node *prev;
    struct Node *next;
} Node;

// Список с сигнальными узлами (sentinel)
// head — фиктивный узел перед первым
// tail — фиктивный узел после последнего
typedef struct {
    Node *head;
    Node *tail;
    size_t size;
} List;

// Создание пустого списка
List* listCreate(void) {
    List *list = malloc(sizeof(List));
    if (!list) return NULL;

    list->head = malloc(sizeof(Node));
    list->tail = malloc(sizeof(Node));
    if (!list->head || !list->tail) {
        free(list->head);
        free(list->tail);
        free(list);
        return NULL;
    }

    // Связываем сигнальные узлы друг с другом
    list->head->next = list->tail;
    list->head->prev = NULL;
    list->tail->prev = list->head;
    list->tail->next = NULL;
    list->size = 0;

    return list;
}

// Вставка нового элемента после узла p
// Возвращает указатель на новый узел
Node* listInsertAfter(List *list, Node *p, int value) {
    Node *newNode = malloc(sizeof(Node));
    if (!new_node) return NULL;

    new_node->value = value;
    new_node->prev = p;
    new_node->next = p->next;

    p->next->prev = newNode;
    p->next = newNode;

    list->size++;
    return new_node;
}

// Вставка в начало (insertFirst)
Node* listInsertFirst(List *list, int value) {
    return listInsertAfter(list, list->head, value);
}

// Вставка в конец (insertLast)
Node* listInsertLast(List *list, int value) {
    return listInsertAfter(list, list->tail->prev, value);
}

// Удаление узла (remove)
// Возвращает удалённое значение
int listRemove(List *list, Node *node) {
    if (node == list->head || node == list->tail) {
        fprintf(stderr, "Ошибка: нельзя удалить сигнальный узел\n");
        exit(1);
    }

    int value = node->value;

    node->prev->next = node->next;
    node->next->prev = node->prev;

    free(node);
    list->size--;

    return value;
}

// Получение значения в позиции (element)
int listElement(Node *node) {
    return node->value;
}

// Переход вперёд (after)
Node* listAfter(List *list, Node *p) {
    if (p->next == list->tail) {
        fprintf(stderr, "Ошибка: p — последняя позиция\n");
        exit(1);
    }
    return p->next;
}

// Переход назад (before)
Node* listBefore(List *list, Node *p) {
    if (p->prev == list->head) {
        fprintf(stderr, "Ошибка: p — первая позиция\n");
        exit(1);
    }
    return p->prev;
}

// Проверка на первую позицию
int listIsFirst(List *list, Node *p) {
    return p->prev == list->head;
}

// Проверка на последнюю позицию
int listIsLast(List *list, Node *p) {
    return p->next == list->tail;
}

// Освобождение всего списка
void listFree(List *list) {
    Node *current = list->head->next;
    while (current != list->tail) {
        Node *next = current->next;
        free(current);
        current = next;
    }
    free(list->head);
    free(list->tail);
    free(list);
}

int main(void) {
    List *list = listCreate();

    // insertFirst — вставка в начало
    listInsertFirst(list, 10);
    listInsertFirst(list, 20);
    listInsertFirst(list, 30);
    // список: 30 <-> 20 <-> 10

    // insertLast — вставка в конец
    listInsertLast(list, 5);
    // список: 30 <-> 20 <-> 10 <-> 5

    // Обход вперёд
    printf("Вперёд:  ");
    for (Node *p = list->head->next; p != list->tail; p = p->next) {
        printf("%d ", p->value);
    }
    printf("\n");

    // Обход назад
    printf("Назад:   ");
    for (Node *p = list->tail->prev; p != list->head; p = p->prev) {
        printf("%d ", p->value);
    }
    printf("\n");

    // Удаляем второй элемент
    Node *second = list->head->next->next;
    int removed = listRemove(list, second);
    printf("Удалён: %d\n", removed);

    // После удаления: 30 <-> 10 <-> 5
    printf("После:   ");
    for (Node *p = list->head->next; p != list->tail; p = p->next) {
        printf("%d ", p->value);
    }
    printf("\n");

    listFree(list);
    return 0;
}
```

### Что важно в этой реализации

**Сигнальные узлы.** `head` и `tail` — фиктивные узлы, которые не хранят данных. Они существуют, чтобы у каждого реального узла были соседи `prev` и `next` — даже у первого и последнего. Без этого пришлось бы писать отдельные ветки для вставки в пустой список, удаления первого элемента и т.д.

**Вставка за `O(1)`.** `list_insert_after` не ищет место — он вставляет узел сразу после указанного. Это ключевое преимущество списка перед массивом.

**Удаление за `O(1)`.** Если есть указатель на узел, `list_remove` работает за константное время. В векторе пришлось бы сдвигать все последующие элементы — `O(n)`.

**Позиция = узел.** В C мы не вводим отдельный тип `Position` — роль позиции играет сам указатель на `Node`. Пользователь может читать `value`, но не должен трогать `prev` и `next` — за это отвечает соглашение, а не язык.

### Сложность операций

| Операция | Сложность |
| :--- | :--- |
| `insertFirst` | `O(1)` |
| `insertLast` | `O(1)` |
| `insertAfter(p)` | `O(1)` |
| `remove(p)` | `O(1)` |
| `element(p)` | `O(1)` |
| `after(p)`, `before(p)` | `O(1)` |
| Поиск элемента по значению | `O(n)` |
| Доступ по разряду | `O(n)` |

Список выигрывает у вектора там, где нужны частые вставки и удаления в середине. Но проигрывает, если нужен доступ по индексу — его приходится искать перебором.

## Стандартные реализации в других языках

Во всех современных языках есть встроенный двусвязный список с аналогичным набором методов.

### C++: `std::list`

```cpp
#include <iostream>
#include <list>

int main() {
    std::list<int> lst = {10, 20, 30};

    lst.push_front(5);    // [5, 10, 20, 30]
    lst.push_back(40);    // [5, 10, 20, 30, 40]

    // Вставка после позиции
    auto it = lst.begin();
    ++it;                 // указывает на 10
    lst.insert(it, 15);   // [5, 15, 10, 20, 30, 40]

    // Удаление по позиции
    it = lst.begin();
    lst.erase(it);        // [15, 10, 20, 30, 40]

    // Обход
    for (int x : lst) {
        std::cout << x << " ";
    }
    std::cout << "\n";

    return 0;
}
```

`std::list` — двусвязный список. Итераторы играют роль позиций. `push_front`, `push_back`, `insert`, `erase` — все `O(1)` при наличии итератора.

### Python: `collections.deque`

В Python встроенный `list` — это динамический массив, а не связный список. Для двусвязного списка используют **`collections.deque`** (двусторонняя очередь).

```python
from collections import deque

lst = deque([10, 20, 30])

lst.appendleft(5)    # [5, 10, 20, 30]
lst.append(40)       # [5, 10, 20, 30, 40]

# Вставка по индексу — O(n), потому что ищется позиция
lst.insert(1, 15)    # [5, 15, 10, 20, 30, 40]

# Удаление с концов — O(1)
lst.pop()            # удаляет 40
lst.popleft()        # удаляет 5

# Обход
for x in lst:
    print(x)
```

`deque` — это двусвязный список блоков. Он даёт `O(1)` на добавление и удаление с обоих концов, но `insert` в середину всё равно `O(n)`, потому что позиция ищется перебором.

> **Замечание:** в Python нет встроенного двусвязного списка с `O(1)`-вставкой по позиции. Это компромисс: `deque` оптимизирован под работу с концами.

### Java: `LinkedList`

```java
import java.util.LinkedList;
import java.util.ListIterator;

public class LinkedListExample {
    public static void main(String[] args) {
        LinkedList<Integer> list = new LinkedList<>();
        list.add(10);
        list.add(20);
        list.add(30);

        list.addFirst(5);       // [5, 10, 20, 30]
        list.addLast(40);       // [5, 10, 20, 30, 40]

        // ListIterator играет роль позиции
        ListIterator<Integer> it = list.listIterator();
        it.next();              // пропускаем 5
        it.add(15);             // вставляем 15 после 5 -> [5, 15, 10, 20, 30, 40]

        // Удаление через итератор
        it.previous();          // возвращаемся к 15
        it.remove();            // удаляем 15

        // Обход
        for (int x : list) {
            System.out.println(x);
        }
    }
}
```

`LinkedList` в Java — классический двусвязный список. Методы `addFirst`, `addLast`, `removeFirst`, `removeLast` работают за `O(1)`. Итератор `ListIterator` — это позиция: через него можно вставлять и удалять за `O(1)`.

### C#: `LinkedList<T>`

```csharp
using System;
using System.Collections.Generic;

class Program {
    static void Main() {
        LinkedList<int> list = new LinkedList<int>();
        list.AddLast(10);
        list.AddLast(20);
        list.AddLast(30);

        list.AddFirst(5);            // [5, 10, 20, 30]

        // LinkedListNode — это позиция
        LinkedListNode<int> node = list.First;
        list.AddAfter(node, 15);     // [5, 15, 10, 20, 30]

        // Удаление по узлу
        list.Remove(node);           // удаляет 5

        // Обход
        foreach (int x in list) {
            Console.WriteLine(x);
        }
    }
}
```

`LinkedList<T>` использует `LinkedListNode<T>` как позицию. Все вставки и удаления по узлу — `O(1)`.

### Сводная таблица

| Язык | Тип | Вставка/удаление по позиции |
| :--- | :--- | :--- |
| **C** | Ручная реализация через `Node*` | `O(1)` |
| **C++** | `std::list` + итераторы | `O(1)` |
| **Python** | `collections.deque` | `O(1)` только с концов |
| **Java** | `LinkedList` + `ListIterator` | `O(1)` |
| **C#** | `LinkedList<T>` + `LinkedListNode<T>` | `O(1)` |

Во всех языках, кроме Python, есть полноценный двусвязный список с позициями. Python-разработчики, которым нужен именно список, обычно реализуют его вручную или используют `deque` с оговорками.

---

## Итог

- Для связных списков **узлы** удобнее разрядов: операции по узлу работают за `O(1)`, а по разряду — `O(n)`.
- **Позиция** — абстракция, скрывающая детали узла (поля `next` и `prev`).
- АТД «Список» поддерживает `first`, `last`, `isFirst`, `isLast`, `before`, `after`, `element`, а также `insertFirst`, `insertLast`, `insertBefore`, `insertAfter`, `remove`, `replaceElement`, `swapElements`.
- В C список реализуется через `struct Node` с полями `prev` и `next`. **Сигнальные узлы** упрощают код.
- Все операции по позиции — `O(1)`; поиск по значению или разряду — `O(n)`.
- В C++ — `std::list`; в Java — `LinkedList`; в C# — `LinkedList<T>`; в Python полноценного списка нет, но есть `deque`.
